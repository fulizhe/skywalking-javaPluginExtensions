# 04 — 真实 metrics.html（A 版式，1h / 6h / 24h）

**What to build:** 用真实指标填充与原型 A 一致的仪表盘：KPI 行、分位带趋势图 + 吞吐/错误图、endpoint 表（含趋势与分级）、下钻到既有链路视图；头部 `1h / 6h / 24h` 三档由分钟分辨率服务，`7d` 暂置灰。

**Blocked by:** 03.

**Status:** ready-for-agent

- [ ] 页面无 OAP 依赖，轮询真实读口；字段与 spec「与原型 A 的字段对齐」一一对应（含全局 `"*"` 分位，不用各 endpoint 分位相加）。
- [ ] 三档范围切换生效，点数受控（不超过上限）。
- [ ] 下钻可跳到既有链路明细视图。
- [ ] 浏览器无 JS 报错；原型文件保留不动。
