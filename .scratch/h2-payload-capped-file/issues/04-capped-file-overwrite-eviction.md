# 04 — 环形文件·覆盖淘汰与"过期"

**What to build:** 写满后自动覆盖最旧块（无需定时清理）；被覆盖的逻辑 id 读回 `null`（过期）；文件物理大小恒定不超过配置上限。

**Blocked by:** 03 — 跨文件尾读写

**Status:** done

- [ ] 持续写入使游标超过文件容量后，最旧 id 读回 `null`
- [ ] 新写入的 id 仍能正常读回（覆盖不破坏新数据）
- [ ] 文件物理大小恒定 ≤ 配置上限
- [ ] 单元测试断言覆盖淘汰与恒定大小
