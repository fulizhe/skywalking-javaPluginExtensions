# 03 — 环形文件·跨文件尾读写

**What to build:** 块可以跨越文件尾：写时写满尾部后从文件头继续；读跨尾块时按同样规则拼接，读写字节一致。

**Blocked by:** 02 — CappedFileStorage 骨架

**Status:** done

- [ ] 单块跨越文件尾写入正确（尾部一段 + 头部一段）
- [ ] 跨尾块读取与写入字节完全一致
- [ ] 单元测试覆盖"紧贴尾部写入触发跨尾"的场景
