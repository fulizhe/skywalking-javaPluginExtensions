# 10 — 背压：队列满丢弃 + 计数暴露

**What to build:** 写队列有界，满时**丢弃并计数**、不阻塞业务线程、不外抛；丢弃计数经对账状态对外可读，便于观测。

**Blocked by:** 09 — 独立写线程 + 有界队列 + close 排空

**Status:** done

- [ ] 队列满时丢弃、不阻塞、不抛异常
- [ ] 丢弃计数可通过对账状态读取（新增字段）
- [ ] 单元/端到端可观测到该计数
