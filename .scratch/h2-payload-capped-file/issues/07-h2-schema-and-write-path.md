# 07 — H2 schema 改 + 写路径接入环形

**What to build:** 明细行不再存整段 JSON：建表**删除载荷列、新增指针列**；写入时把 JSON 压缩写进环形文件，再把指针写入 H2。旧内存路径与一切对外输出不变。

**Blocked by:** 06 — fsync / close / 并发

**Status:** done

- [ ] 建表删除载荷列、新增指针列；INSERT/SELECT 语句同步调整（mem 模式重建表）
- [ ] 写入把 JSON 压缩落环形并把返回的指针写入 H2
- [ ] 单元测试断言：写入后 H2 有指针行、环形有可回读的对应块
